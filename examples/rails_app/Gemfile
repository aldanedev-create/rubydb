source "https://rubygems.org"

gem "rails", "~> 7.2.0"
# Keep the example compatible with Windows Ruby builds where the newest rbs
# native extension may not link against the bundled toolchain.
gem "irb", "~> 1.16.0"
gem "rdoc", "< 8.0"
gem "rubydb", path: "../.."
gem "rubydb-activerecord", path: "../../adapters/activerecord"
gem "puma", "~> 6.0"
gem "tzinfo-data", platforms: %i[windows jruby]
